dependencies:
    pip install python-dotenv PyYAML websockets python-telegram-bot aiohttp requests matplotlib mplfinance pandas Pillow 
you need to create a bot using telegram botfather and place its api key in the .env file
you can change the hotwallet from the .env file
when everything is set run the main.py file